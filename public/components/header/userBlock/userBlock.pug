.user-block
    a.user-block__profile-href(href=`${profileHref}/${userId}`)
        img.user-block__img(src=imgSrc)
    .user-block__submenu
        span #{userName}
        a.user-block__profile(href=`${profileHref}/${userId}`) Профиль
        div.user-block__logout-btn.not-route Выйти
